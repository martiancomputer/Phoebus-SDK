#ifndef __RTL83XX_IOCTL_H_
#define __RTL83XX_IOCTL_H_

/* RTK switch Command  */
typedef enum
{
	RTK_SWITCH_INIT = 1,
	RTK_SWITCH_MAX_PKTLEN_SET,
	RTK_SWITCH_MAX_PKTLEN_GET,
	/* RTK_SWITCH_PORT_MAX_PKTLEN_SET, */
	/* RTK_SWITCH_PORT_MAX_PKTLEN_GET, */
	/* RTK_SWITCH_GREEN_ETHERNET_SET, */
	/* RTK_SWITCH_GREEN_ETHERNET_GET, */
	/* RTK_RATE_SHAREMETER_SET, */
	/* RTK_RATE_SHAREMETER_GET, */
	/* RTK_RATE_SHAREMETER_BUCKET_SET, */
	/* RTK_RATE_SHAREMETER_BUCKET_GET, */
	RTK_RATE_IGR_BW_SET,
	RTK_RATE_IGR_BW_GET,
	RTK_RATE_EGR_BW_SET,
	RTK_RATE_EGR_BW_GET,
	RTK_RATE_EGR_QBW_EN_SET,
	RTK_RATE_EGR_QBW_EN_GET,
	RTK_RATE_EGR_QBW_SET,
	RTK_RATE_EGR_QBW_GET,
	RTK_STORM_CTRL_RATE_SET,
	RTK_STORM_CTRL_RATE_GET,
	RTK_STORM_BYPASS_SET,
	RTK_STORM_BYPASS_GET,
	RTK_QOS_INIT,
	RTK_QOS_PRI_SEL_SET,
	RTK_QOS_PRI_SEL_GET,
	RTK_QOS_DOT1P_PRI_REMAP_SET,
	RTK_QOS_DOT1P_PRI_REMAP_GET,
	RTK_QOS_DSCP_PRI_REMAP_SET,
	RTK_QOS_DSCP_PRI_REMAP_GET,
	RTK_QOS_PORT_PRI_SET,
	RTK_QOS_PORT_PRI_GET,
	RTK_QOS_QUE_NUM_SET,
	RTK_QOS_QUE_NUM_GET,
	RTK_QOS_PRI_MAP_SET,
	RTK_QOS_PRI_MAP_GET,
	RTK_QOS_SCHE_QUE_SET,
	RTK_QOS_SCHE_QUE_GET,
	RTK_QOS_DOT1P_REMARK_EN_SET,
	RTK_QOS_DOT1P_REMARK_EN_GET,
	RTK_QOS_DOT1P_REMARK_SET,
	RTK_QOS_DOT1P_REMARK_GET,
	RTK_QOS_DSCP_REMARK_EN_SET,
	RTK_QOS_DSCP_REMARK_EN_GET,
	RTK_QOS_DSCP_REMARK_SET,
	RTK_QOS_DSCP_REMARK_GET,
	/* RTK_TRAP_UNKNOWNUNICASTPKTACTION_SET, */
	/* RTK_TRAP_UNKNOWNUNICASTPKTACTION_GET, */
	/* RTK_TRAP_UNKNOWNMCASTPKTACTION_SET, */
	/* RTK_TRAP_UNKNOWNMCASTPKTACTION_GET, */
	/* RTK_TRAP_IGMPCTRLPKTACTION_SET, */
	/* RTK_TRAP_IGMPCTRLPKTACTION_GET, */
	/* RTK_TRAP_RMAACTION_SET, */
	/* RTK_TRAP_RMAACTION_GET, */
	/* RTK_TRAP_ETHERNETAV_SET, */
	/* RTK_TRAP_ETHERNETAV_GET, */
	/* RTK_LEAKY_VLAN_SET, */
	/* RTK_LEAKY_VLAN_GET, */
	/* RTK_LEAKY_PORTISOLATION_SET, */
	/* RTK_LEAKY_PORTISOLATION_GET, */
	RTK_PORT_PHY_AN_ABILITY_SET,
	RTK_PORT_PHY_AN_ABILITY_GET,
	RTK_PORT_PHY_FORCE_ABILITY_SET,
	RTK_PORT_PHY_FORCE_ABILITY_GET,
	RTK_PORT_PHY_STATUS_GET,
	/* RTK_PORT_PHYTESTMODE_SET, */
	/* RTK_PORT_PHYTESTMODE_GET, */
	/* RTK_PORT_PHY1000BASETMASTERSLAVE_SET, */
	/* RTK_PORT_MACFORCELINK_SET, */
	/* RTK_PORT_MACFORCELINK_GET, */
	/* RTK_PORT_MACFORCELINKEXT0_SET, */
	/* RTK_PORT_MACFORCELINKEXT0_GET, */
	/* RTK_PORT_MACFORCELINKEXT1_SET, */
	/* RTK_PORT_MACFORCELINKEXT1_GET, */
	RTK_PORT_MACFORCELINKEXT_SET,
	RTK_PORT_MACFORCELINKEXT_GET,
	RTK_PORT_MACSTATUS_GET,
	/* RTK_PORT_MACLOCALLOOPBACKENABLE_SET, */
	/* RTK_PORT_MACLOCALLOOPBACKENABLE_GET, */
	RTK_PORT_PHYREG_SET,
	RTK_PORT_PHYREG_GET,
	/* RTK_PORT_BACKPRESSUREENABLE_SET, */
	/* RTK_PORT_BACKPRESSUREENABLE_GET, */
	RTK_PORT_ADMIN_STATE_SET,
	RTK_PORT_ADMIN_STATE_GET,
	RTK_PORT_ISOLATION_SET,
	RTK_PORT_ISOLATION_GET,
	RTK_PORT_RGMIIDELAYEXT0_SET,
	RTK_PORT_RGMIIDELAYEXT0_GET,
	RTK_PORT_RGMIIDELAYEXT1_SET,
	RTK_PORT_RGMIIDELAYEXT1_GET,
	RTK_PORT_RGMIIDELAYEXT_SET,
	RTK_PORT_RGMIIDELAYEXT_GET,
	RTK_PORT_ENABLE_ALL_SET,
	RTK_PORT_ENABLE_ALL_GET,
	/* RTK_PORT_EFID_SET, */
	/* RTK_PORT_EFID_GET, */
	/* RTK_PORT_PHYCOMBOPORTMEDIA_SET, */
	/* RTK_PORT_PHYCOMBOPORTMEDIA_GET, */
	RTK_VLAN_INIT,
	RTK_VLAN_SET,
	RTK_VLAN_GET,
	RTK_VLAN_PVID_SET,
	RTK_VLAN_PVID_GET,
	RTK_VLAN_PORT_IGRFILTER_EN_SET,
	RTK_VLAN_PORT_IGRFILTER_EN_GET,
	RTK_VLAN_PORT_AFT_SET,
	RTK_VLAN_PORT_AFT_GET,
	RTK_VLAN_BASED_PRI_SET,
	RTK_VLAN_BASED_PRI_GET,
	RTK_VLAN_TAGMODE_SET,
	RTK_VLAN_TAGMODE_GET,
	/* RTK_VLAN_TRANSPARENT_SET, */
	/* RTK_VLAN_TRANSPARENT_GET, */
	/* RTK_VLAN_STG_SET, */
	/* RTK_VLAN_STG_GET, */
	/* RTK_VLAN_PPBASED_VLAN_ADD, */
	/* RTK_VLAN_PPBASED_VLAN_GET, */
	/* RTK_VLAN_PPBASED_VLAN_DEL, */
	/* RTK_VLAN_PPBASED_VLAN_DEL_ALL, */
	/* RTK_VLAN_PORTFID_SET, */
	/* RTK_VLAN_PORTFID_GET, */
	RTK_VLAN_MBR_FILTER_SET,
	RTK_VLAN_MBR_FILTER_GET,
	RTK_STP_INIT,
	RTK_STP_MSTP_STATE_SET,
	RTK_STP_MSTP_STATE_GET,
	RTK_L2_INIT,
	RTK_L2_ADDR_ADD,
	RTK_L2_ADDR_GET,
	RTK_L2_ADDR_NEXT_GET,
	RTK_L2_ADDR_DEL,
	RTK_L2_MCADDR_ADD,
	RTK_L2_MCADDR_GET,
	RTK_L2_MCADDR_NEXT_GET,
	RTK_L2_MCADDR_DEL,
	/* RTK_L2_IPMCADDR_ADD, */
	/* RTK_L2_IPMCADDR_GET, */
	/* RTK_L2_IPMCADDR_NEXT_GET, */
	/* RTK_L2_IPMCADDR_DEL, */
	/* RTK_L2_FLUSH_TYPE_SET, */
	/* RTK_L2_FLUSH_LKDN_SET, */
	/* RTK_L2_FLUSH_LKDN_GET, */
	RTK_L2_AGING_ENABLE_SET,
	RTK_L2_AGING_ENABLE_GET,
	RTK_L2_LRN_LMT_SET,
	RTK_L2_LRN_LMT_GET,
	RTK_L2_LRN_ACTION_SET,
	RTK_L2_LRN_ACTION_GET,
	RTK_L2_LRN_CNT_GET,
	RTK_L2_FLOOD_PORTS_SET,
	RTK_L2_FLOOD_PORTS_GET,
	RTK_L2_LOCALPKT_PMT_SET,
	RTK_L2_LOCALPKT_PMT_GET,
	RTK_L2_AGING_SET,
	RTK_L2_AGING_GET,
	/* RTK_L2_IPMCADDR_LKUP_SET, */
	/* RTK_L2_IPMCADDR_LKUP_GET, */
	/* RTK_L2_ENTRY_GET, */
	RTK_SVLAN_INIT,
	RTK_SVLAN_SVC_PORT_ADD,
	RTK_SVLAN_SVC_PORT_GET,
	RTK_SVLAN_SVC_PORT_DEL,
	/* RTK_SVLAN_TPID_SET, */
	/* RTK_SVLAN_TPID_GET, */
	/* RTK_SVLAN_PRI_REF_SET, */
	/* RTK_SVLAN_PRI_REF_GET, */
	RTK_SVLAN_MBRPORT_ENTRY_SET,
	RTK_SVLAN_MBRPORT_ENTRY_GET,
	RTK_SVLAN_DEF_SVID_SET,
	RTK_SVLAN_DEF_SVID_GET,
	/* RTK_SVLAN_C2S_ADD, */
	/* RTK_SVLAN_C2S_DEL, */
	/* RTK_SVLAN_C2S_GET, */
	/* RTK_SVLAN_UNTAG_ACTION_SET, */
	/* RTK_SVLAN_UNTAG_ACTION_GET, */
	RTK_SVLAN_UNMATCH_ACTION_SET,
	RTK_SVLAN_UNMATCH_ACTION_GET,
	/* RTK_SVLAN_DMAC_VIDSEL_SET, */
	/* RTK_SVLAN_DMAC_VIDSEL_GET, */
	/* RTK_SVLAN_IPMC2S_ADD, */
	/* RTK_SVLAN_IPMC2S_DEL, */
	/* RTK_SVLAN_IPMC2S_GET, */
	/* RTK_SVLAN_L2MC2S_ADD, */
	/* RTK_SVLAN_L2MC2S_DEL, */
	/* RTK_SVLAN_L2MC2S_GET, */
	/* RTK_SVLAN_SP2C_ADD, */
	/* RTK_SVLAN_SP2C_GET, */
	/* RTK_SVLAN_SP2C_DEL, */
	RTK_CPU_ENABLE_SET,
	RTK_CPU_ENABLE_GET,
	RTK_CPU_TAG_PORT_SET,
	RTK_CPU_TAG_PORT_GET,
	RTK_CPU_TAG_POSITION_SET,
	RTK_CPU_TAG_POSITION_GET,
	/* RTK_DOT1X_UNAUTHPACKETOPER_SET, */
	/* RTK_DOT1X_UNAUTHPACKETOPER_GET, */
	/* RTK_DOT1X_EAPOLFRAME2CPUENABLE_SET, */
	/* RTK_DOT1X_EAPOLFRAME2CPUENABLE_GET, */
	/* RTK_DOT1X_PORTBASEDENABLE_SET, */
	/* RTK_DOT1X_PORTBASEDENABLE_GET, */
	/* RTK_DOT1X_PORTBASEDAUTHSTATUS_SET, */
	/* RTK_DOT1X_PORTBASEDAUTHSTATUS_GET, */
	/* RTK_DOT1X_PORTBASEDDIRECTION_SET, */
	/* RTK_DOT1X_PORTBASEDDIRECTION_GET, */
	/* RTK_DOT1X_MACBASEDENABLE_SET, */
	/* RTK_DOT1X_MACBASEDENABLE_GET, */
	/* RTK_DOT1X_MACBASEDAUTHMAC_ADD, */
	/* RTK_DOT1X_MACBASEDAUTHMAC_DEL, */
	/* RTK_DOT1X_MACBASEDDIRECTION_SET, */
	/* RTK_DOT1X_MACBASEDDIRECTION_GET, */
	/* RTK_DOT1X_GUESTVLAN_SET, */
	/* RTK_DOT1X_GUESTVLAN_GET, */
	/* RTK_DOT1X_GUESTVLAN2AUTH_SET, */
	/* RTK_DOT1X_GUESTVLAN2AUTH_GET, */
	/* RTK_TRUNK_PORT_SET, */
	/* RTK_TRUNK_PORT_GET, */
	/* RTK_TRUNK_DISTRIBUTIONALGORITHM_SET, */
	/* RTK_TRUNK_DISTRIBUTIONALGORITHM_GET, */
	/* RTK_TRUNK_QEUEUEMPTYSTATUS_GET, */
	RTK_MIRROR_PORT_BASED_SET,
	RTK_MIRROR_PORT_BASED_GET,
	RTK_MIRROR_PORT_ISO_SET,
	RTK_MIRROR_PORT_ISO_GET,
	RTK_STAT_GLOBAL_RESET,
	RTK_STAT_PORT_RESET,
	RTK_STAT_GLOBAL_GET,
	RTK_STAT_GLOBAL_GETALL,
	RTK_STAT_PORT_GET,
	RTK_STAT_PORT_GETALL,
	/* RTK_INT_POLARITY_SET, */
	/* RTK_INT_POLARITY_GET, */
	/* RTK_INT_CONTROL_SET, */
	/* RTK_INT_CONTROL_GET, */
	/* RTK_INT_STATUS_SET, */
	/* RTK_INT_STATUS_GET, */
	/* RTK_INT_ADVANCEINFO_GET, */
	/* RTK_LED_ENABLE_SET, */
	/* RTK_LED_ENABLE_GET, */
	/* RTK_LED_OPERATION_SET, */
	/* RTK_LED_OPERATION_GET, */
	/* RTK_LED_MODE_SET, */
	/* RTK_LED_MODE_GET, */
	/* RTK_LED_MODEFORCE_SET, */
	/* RTK_LED_MODEFORCE_GET, */
	/* RTK_LED_BLINKRATE_SET, */
	/* RTK_LED_BLINKRATE_GET, */
	/* RTK_LED_GROUPCONFIG_SET, */
	/* RTK_LED_GROUPCONFIG_GET, */
	/* RTK_LED_SERIALMODE_SET, */
	/* RTK_LED_SERIALMODE_GET, */
	RTK_ACL_INIT,
	/* RTK_ACL_FIELD_ADD, */
	RTK_ACL_CFG_ADD,
	RTK_ACL_CFG_DEL,
	RTK_ACL_CFG_DELALL,
	RTK_ACL_CFG_GET,
	RTK_ACL_UMACTION_SET,
	RTK_ACL_UMACTION_GET,
	RTK_ACL_STATE_SET,
	RTK_ACL_STATE_GET,
	/* RTK_ACL_TEMPLATE_SET, */
	/* RTK_ACL_TEMPLATE_GET, */
	/* RTK_ACL_FIELD_SEL_SET, */
	/* RTK_ACL_FIELD_SEL_GET, */
	/* RTK_FILTER_IPRANGE_SET, */
	/* RTK_FILTER_IPRANGE_GET, */
	/* RTK_FILTER_VIDRANGE_SET, */
	/* RTK_FILTER_VIDRANGE_GET, */
	/* RTK_FILTER_PORTRANGE_SET, */
	/* RTK_FILTER_PORTRANGE_GET, */
	RTK_EEE_INIT,
	/* RTK_EEE_PORTEN_SET, */
	/* RTK_EEE_PORTEN_GET, */
	RTK_IGMP_INIT,
	RTK_IGMP_STATE_SET,
	RTK_IGMP_STATE_GET,
	RTK_IGMP_STATIC_ROUTER_PORT_SET,
	RTK_IGMP_STATIC_ROUTER_PORT_GET,
	RTK_IGMP_PROTOCOL_SET,
	RTK_IGMP_PROTOCOL_GET,
	RTK_MAX,
	PHY_REG_READ,
	PHY_REG_WRITE,
	SWITCH_REG_READ,
	SWITCH_REG_WRITE,
	RTK_STAT_PORT_SDS_RESET,
	RTK_STAT_PORT_SGMII_LINK_STATUS_GET,
} RTK_COMMAND_DEF;


#endif /* __RTL83XX_IOCTL_H_ */

