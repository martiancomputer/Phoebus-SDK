# Phoebus /etc/shadow -- TEMPLATE (public repo).
# Both accounts are LOCKED (*). Real password hashes are injected at
# build time from a gitignored secrets file -- see PROVISIONING.md.
root:*:20659:0:99999:7:::
admin:*:20659:0:99999:7:::
